import express from "express"; import { requireAuth } from "../lib/auth.js"; import { getTaskCatalog, runTask } from "../lib/task-runner.js"; import { createJob, finishJob } from "../lib/jobs.js"; const router=express.Router(); router.get('/', (_req,res)=>res.json({ok:true,tasks:getTaskCatalog()})); router.post('/run', requireAuth, async (req,res)=>{ const {task,payload}=req.body||{}; const job=createJob({task,payload,status:'running'}); try{ const result=await runTask(task,payload||{}); const done=finishJob(job.id,'done',result); res.json({ok:true,job:done,result}); } catch(error){ const failed=finishJob(job.id,'failed',{error:error.message}); res.status(400).json({ok:false,job:failed,error:error.message}); }}); export default router;