import express from "express"; import { searchWorkspace } from "../lib/search.js"; const router=express.Router(); router.get('/', (req,res)=>{ try{ const q=String(req.query.q||'').trim(); if(!q) return res.json({ok:true,results:[]}); res.json({ok:true,query:q,results:searchWorkspace(q)}); } catch(error){ res.status(500).json({ok:false,error:error.message}); }}); export default router;