import express from "express"; import { listDocs, readDoc } from "../lib/docs.js"; const router=express.Router(); router.get('/', (_req,res)=>{ try{ res.json({ok:true,docs:listDocs()}); } catch(error){ res.status(500).json({ok:false,error:error.message}); }}); router.get('/read', (req,res)=>{ try{ const name=String(req.query.name||'operator_quickstart.md'); res.json({ok:true,name,content:readDoc(name)}); } catch(error){ res.status(400).json({ok:false,error:error.message}); }}); export default router;