# one2lvos_v4

Atomic, recovery-first system image.

This release is meant to reduce human error by shipping a full standalone build instead of patch instructions.

## Start

```bash
cp .env.example .env
./install.sh
./start.sh
```

Open `http://localhost:8080`
